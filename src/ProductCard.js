import React from 'react';
import { Card, CardMediam, CardContent, Typography } from '@material-ui/core';

export default funtion ProductCard (props) {
    return (
        <Card>
            <CardMedia />
            <CardContent>
                <Typography>Nombre del producto deportivo 1</Typography>
            </CardContent>
        </Card>
    );
};